<!-- Divider Component -->
<mj-divider 
  border-color="{{ borderColor | default: theme_border_color | default: '#eeeeee' }}" 
  border-width="{{ borderWidth | default: '1px' }}" 
  padding="{{ padding | default: '30px 0' }}" 
/>