<!-- Common Header -->
<mj-section background-color="{{ theme_header_bg | default: '#000000' }}" padding="20px 0">
  <mj-column>
    <mj-text align="center" color="{{ theme_header_text | default: '#ffffff' }}" font-size="24px" font-weight="{{ theme_heading_weight | default: 'bold' }}">
      {{ appName | default: "CrunchyCone" }}
    </mj-text>
  </mj-column>
</mj-section>