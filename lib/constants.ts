/**
 * UI Constants
 * Centralized constants for UI dimensions and spacing
 */

/**
 * Fixed admin header height in pixels
 * Includes: 50px content height + 5px top padding + 5px bottom padding = 60px total
 */
export const ADMIN_HEADER_HEIGHT = 60;

/**
 * Fixed admin header content height (without padding)
 */
export const ADMIN_HEADER_CONTENT_HEIGHT = 50;

/**
 * Fixed admin header padding
 */
export const ADMIN_HEADER_PADDING = 5;
